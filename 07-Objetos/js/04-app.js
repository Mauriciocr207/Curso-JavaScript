// destructuring de objetos
const producto = {
    nombre: "Monitor",
    precio: 300,
    disponible: true,
}

const {nombre, precio, disponible} = producto;

console.log(nombre);
console.log(precio);
console.log(disponible);